spring:
  datasource:
    password: test
    username: test
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: update

  cloud:
    config:
      import-check:
         enabled: false
